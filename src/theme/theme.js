export const theme = {
  colors: {
    primary: "#FAFAF7",     // Clean cream white (Background)
    secondary: "#F2B705",   // Bright saffron yellow (Brand)
    accent: "#FFF0C2",      // Soft highlight
    text: "#2E2E2E",        // Neutral dark text
    textMuted: "#6B7280",   // Muted text (gray-500 equivalent)
    muted: "#EAE6D8",       // Soft separators
    surface: "#FFFFFF",     // Pure white for cards
    dark: "#1A1A1A",        // Dark surface for footer/etc
    border: "rgba(242, 183, 5, 0.1)", // Subtle gold border
    shadow: "rgba(0, 0, 0, 0.08)",     // Subtle soft shadow
  },
  fonts: {
    heading: "'Cormorant Garamond', serif",
    body: "'Outfit', sans-serif",
    accent: "'Cinzel', serif"
  }
};
